# 项目状态

更新时间：2026-08-28

## 当前阶段

计划书一级、二级仿真闭环已完成；三级 PPO 扩展已完成工程链路、分阶段课程
训练和固定 30 回合评估。随机空场尚未达到晋级线；稳定控制基线为单点导航
Nav2 DWB、多点巡航 Nav2 Regulated Pure Pursuit，PPO 未替换交付控制器。

## 已确认环境

- WSL2：Ubuntu 22.04
- ROS2：Humble
- Gazebo Classic：11.10.2
- 可用组件：`gazebo_ros`、`gazebo_plugins`、`xacro`、
  `robot_state_publisher`、`cv_bridge`、`image_transport`、
  `slam_toolbox`、`nav2_map_server`、`navigation2`、`nav2_bringup`
- 暂缺组件：`turtlebot3_gazebo`（本项目不依赖）

## 已完成

- 建立 Ubuntu 原生构建工作空间：
  `/home/lddcyfy/zhirong_xingzhe_ws`
- 建立六个功能包：
  - `zhirong_description`
  - `zhirong_gazebo`
  - `zhirong_bringup`
  - `zhirong_vision`
  - `zhirong_tasks`
  - `zhirong_ppo`
- 完成四轮滑移转向底盘 Xacro 模型：
  - 车体
  - 左前、右前、左后、右后四个驱动轮
  - 不使用辅助轮或支撑轮
  - Gazebo 四轮滑移转向驱动配置
- 完成测试世界和两个静态障碍物。
- 完成一键启动文件 `simulation.launch.py`。
- 完成车顶二维激光雷达：
  - 360° 扫描
  - 10 Hz
  - 每圈 720 点
  - 0.12～12 m 测距范围
  - 发布 `/scan`，坐标系为 `lidar_link`
- 完成 RViz2 配置：
  - RobotModel
  - TF
  - Odometry
  - LaserScan
  - RGB-D PointCloud
- 完成前置 RGB-D 相机：
  - 320×240 分辨率
  - 15 Hz 彩色图与深度图
  - 70° 水平视场角
  - 0.15～8 m 深度范围
  - 发布彩色图、深度图、相机参数和带 RGB 点云
  - 使用标准 `camera_optical_link` 光学坐标系
- 相机外壳已放在雷达扫描平面下方，不会遮挡二维雷达。
- 图形化一键启动脚本现在会同时启动 Gazebo 和 RViz2。
- 安装 ROS2 Humble `slam_toolbox` 及所需依赖：
  - 新增 56 个软件包
  - 约占用 296 MB
  - 同时获得 `nav2_map_server` 地图保存工具
- 完成 SLAM 参数 `slam_toolbox_mapping.yaml`：
  - `map -> odom -> base_footprint`
  - 地图分辨率 `0.05 m/cell`
  - 雷达范围 `0.12～12 m`
  - 在线异步建图和闭环检测
- 完成 `mapping.launch.py` 和 `start_mapping_gui.sh`。
- 完成建图版 RViz2：
  - 固定坐标系 `map`
  - 二维地图、RobotModel、TF、LaserScan 和 Odometry
- Gazebo 测试场景增加四面围墙，形成可验证的封闭建图区域。
- 完成地图验证与自动方形路线脚本。
- 已保存：
  - `maps/zhirong_test_map.pgm`
  - `maps/zhirong_test_map.yaml`
  - `maps/zhirong_test_map_preview.png`
- 安装完整 ROS2 Humble Nav2：
  - 新增 43 个软件包
  - 约占用 52.6 MB
- 完成 `nav2_params.yaml`：
  - AMCL 使用 `/scan` 和保存地图定位
  - `base_footprint` 四轮差速运动模型
  - 机器人安全半径 `0.26 m`
  - NavFn 全局规划器
  - 单点导航 DWB 局部控制器
  - 多点巡航 Regulated Pure Pursuit 路径跟踪器
  - 全局/局部障碍物层与膨胀层
  - 最高导航线速度 `0.30 m/s`
  - 单点目标位置容差 `0.08 m`
- 完成 `navigation.launch.py` 和 `start_navigation_gui.sh`。
- 地图在构建时安装到 WSL 纯英文运行路径，解决 Humble launch 对中文路径的转义问题。
- 完成智融行者专用 `zhirong_navigation.rviz`：
  - 地图和机器人模型
  - AMCL 粒子
  - 激光扫描
  - 全局/局部代价地图
  - 全局/局部路径
  - `2D Pose Estimate` 和 `Nav2 Goal`
  - `Navigation 2` 取消导航面板
- 完成自动目标点测试脚本。
- 完成松手即停图形键盘控制器 `keyboard_hold_teleop.py`：
  - 按住 `W/S` 前进或倒车，松开停车
  - 按住 `A/D` 左转或右转，松开停止转向
  - 支持前进、倒车和转向速度滑条
  - `空格`、窗口失焦或关闭时自动停车
- 已安装手柄组件 `joy` 和 `teleop_twist_joy`。
- 手柄控制按用户要求暂时搁置，当前不启动手柄控制桥。
- 已输出：
  - `/cmd_vel`
  - `/odom`
  - `/scan`
  - `/tf`

## 验证结果

- `colcon build --symlink-install`：6 个包编译成功。
- Xacro：可正常展开为 URDF。
- Gazebo：机器人成功生成。
- 四轮运动自动测试：
  - 测试速度：`0.25 m/s`
  - 位移变化：`0.7144 m`
  - 转向指令：`angular.z = 0.8 rad/s`
  - 姿态四元数 Z 变化：`0.6976`
- 激光雷达自动测试：
  - 帧名：`lidar_link`
  - 每圈点数：`720`
  - 最近障碍物：`1.838 m`
  - 实测发布频率：约 `9.98 Hz`
  - 有效测距点、扫描角度与量程检查通过
- RGB-D 相机自动测试：
  - 彩色图：`320x240 rgb8`
  - 深度图：`320x240 32FC1`
  - 点云：`320x240`，包含 `rgb`、`x`、`y`、`z`
  - 彩色图实测发布频率：约 `15.0 Hz`
  - 点云在当前 WSL 图形负载下实测约 `7 Hz`
  - 彩色图、深度图、相机参数和点云数据检查通过
  - RViz2 已目视确认点云与 Gazebo 障碍物位置一致
- TF：
  - 已观察到 `odom -> base_footprint`
  - `base_link -> lidar_link` 为固定变换，Z 高度 `0.145 m`
  - `base_link -> camera_optical_link` 固定变换与标准光学轴方向正确
- 完整链路：底盘、里程计、雷达、RGB-D、点云和 TF 自动验证通过。
- SLAM 自动测试：
  - 初始已知地图区域：`5762` 格
  - 方形路线后已知区域：`13783` 格
  - 最终地图：`119 × 119`
  - 分辨率：`0.05 m/cell`
  - 自由区域：`13278` 格
  - 占用区域：`505` 格
  - `map -> odom` TF 严格检查通过
  - 地图文件保存成功
  - `MAPPING_SMOKE_TEST_OK`
- SLAM 图形界面：
  - RViz2 `Map` 状态为 `OK`
  - 地图、围墙、障碍物和机器人位置已目视确认一致
  - 松手即停键盘窗口保持可用
- Nav2 自动测试：
  - 测试目标：`map (0.8, 0.0)`
  - 实际行驶距离：`0.695 m`
  - 最终近似误差：`0.105 m`
  - 行为树结果：`SUCCEEDED`
  - `/navigate_to_pose`、规划器、控制器和速度平滑器工作正常
  - `NAVIGATION_VALIDATION_OK`
  - `NAVIGATION_SMOKE_TEST_OK`
- 当前导航运行态：
  - AMCL：`active`
  - BT Navigator：`active`
  - 全局/局部代价地图分辨率：`0.05 m/cell`
  - `map -> odom` TF 正常
  - 专用 RViz2 进程已启动
- 松手即停控制器：
  - 按键速度映射测试通过
  - Tk `KeyPress`/`KeyRelease` 图形事件测试通过
  - 待机 `/cmd_vel` 线速度和角速度均为 `0`
- 图形界面：Gazebo、RViz2 和松手即停键盘控制窗口均可在 xRDP 桌面显示。

## 当前边界

- 已完成稳定底盘、二维雷达、RGB-D、SLAM 和保存地图上的单目标自主导航。
- 当前全部验证均为仿真验证，没有连接或测试实体硬件。
- 导航模式与键盘控制模式不能同时运行，避免两个节点同时发布 `/cmd_vel`。
- 旧工作空间 `roarm_ws_em0` 不作为本项目依赖；构建时只加载
  `/opt/ros/humble` 和本项目工作空间。

## 下一步

1. 增加多个航点巡航与一键返航。
2. 在 Gazebo 中加入移动障碍物，验证实时绕行和重新规划。
3. 接入 Nav2 Collision Monitor，增加方向感知碰撞保护。
4. 记录规划时间、到达误差、避障距离和任务成功率。
5. 手柄控制继续保持暂停，用户决定恢复时再验证。

## 最终集成状态（2026-07-29）

上面的“下一步”第 1～4 项已经完成，项目进入计划书定义的完整仿真交付状态：

- 新增 `zhirong_vision`：
  - 红、橙、绿、蓝颜色识别
  - `NAV:HOME` 二维码实景解码
  - 连续帧去抖、事件冷却和调试图
- 新增 `zhirong_tasks`：
  - 文本/JSON 命令
  - 任务队列和状态机
  - 多点巡航、一键返航、取消和清队列
  - 不可达目标重试一次、失败上报和无需重启恢复
  - 一次性视觉授权，首个有效映射触发后自动撤权
- 新增动态障碍和 `gazebo_ros_state`：
  - 测试可移动红色动态圆柱
  - `/set_entity_state` 和 `/model_states`
- 接入 Nav2 Collision Monitor：
  - `/cmd_vel -> /cmd_vel_safe -> 底盘`
  - 按当前速度方向预测车体轨迹碰撞
  - 前进路径受阻时减速直至停止；旋转或后退路径安全时继续执行
- 新增导航目标安全入口：
  - `/navigate_to_pose` 由 Goal Guard 唯一接收
  - 目标距障碍不足 `0.34 m`、位于未知区域或地图外时立即拒绝
  - 内部 Nav2 动作隔离为 `/nav2_raw/navigate_to_pose`
  - 脱困限定为安全后退 `0.20 m`、安全旋转 `45°`，随后明确失败
- 新增 `system.launch.py` 和 `start_full_system_gui.sh`。
- 新增 `full_regression_test.sh` 和持久化验收日志。

关键实测：

- 四站点巡航：40.223 秒，返航误差 `0.083 m`
- 动态避障：目标误差 `0.076 m`，横向绕行 `0.240 m`
- 动态障碍最近中心距离：`0.731 m`
- 危险目标 `(2.22, -1.16)`：移动前拒绝，检测到最近致命障碍约 `0.09 m`
- 方向感知安全层：受阻前进最大 `0.017 m/s`，位移 `0.018 m`
- 障碍在前方时原地转向：安全角速度 `0.500 rad/s`
- 后方安全时后退：安全速度 `0.120 m/s`
- 障碍移开后恢复速度：`0.200 m/s`
- `NAV:HOME` 视觉返航：37.936 秒，返航误差 `0.036 m`
- 不可达目标：自动执行 2 次尝试，随后有效任务恢复成功
- 转向稳定性门禁：约 `88.5°` 原地转向时，里程计最大误差 `0.0104 m`、
  AMCL 最大单步修正 `0.0091 m`、航向均方误差 `0.0417 rad`，全部通过

仍然保持的边界：

- 手柄阶段按用户要求暂停。
- PPO、语音、VLA/VLM、机械臂和实车属于三级扩展。
- 当前所有运动验收均为 Gazebo 仿真，实体硬件尚未测试。

### 最终收口复验

- 完整系统生命周期健康检查：`FULL_SYSTEM_HEALTH_OK`
- 视觉巡检：`blue_station → qr_station → home`
- 巡检用时：`50.837 s`
- 最终返航误差：`0.050 m`
- 单任务超时：`90 s`，超时后取消并按配置重试
- 最终演示：`artifacts/demo/zhirong_complete_demo_final.mp4`
- 演示文件：320×240、15 FPS、1051 帧、70.067 秒

## 运动平顺性与四轮差速标定（2026-08-27）

已完成：

- 明确 RViz 红色曲线是 `/plan` 全局规划，不是车辆实际行驶轨迹；新增
  `measure_straight_motion.py`，直接使用 `/odom`、`/cmd_vel_safe` 和
  `/plan` 量化实际轨迹、响应延迟及转向反复。
- 点到点验收改为自动执行“回原点 → 直行 0.8 m → 回原点”，并增加
  横向偏差 `≤ 0.08 m`、路径比 `≤ 1.12`、响应 `≤ 0.40 s`、转向反复
  `≤ 4` 的自动门禁。
- Nav2 终点容差收紧到位置 `0.08 m`、方向 `0.05 rad`；速度平滑频率
  提高到 `40 Hz`，DWB 转向采样增加到 24 组。
- 四轮模型改为滚动方向高摩擦、横向低摩擦，驱动有效轮距标定为
  `0.40 m`；物理轮距仍为 `0.34 m`。
- Progress Checker 改为 `0.05 m / 20 s`，允许四轮底盘先原地掉头，
  避免把正常方向调整误判为“没有进展”。
- AMCL 更新阈值从 `0.05` 降到 `0.02`，激光束增加到 180，最少粒子数
  增加到 800，降低转向时一次性位置修正。

最终仿真实测：

- 点到点面板验收：合格，自动返航成功。
- 调参对比中的直线最大横向偏差：`0.0497 m → 0.0283 m`；响应延迟
  保持约 `0.021 s`。
- 原地转向：指令理论转角约 `2.40 rad`，Gazebo 实际 `2.4499 rad`。
- AMCL 最大单步位置修正：`0.0340 m → 0.0069 m`。
- 动态避障：合格；障碍中心最近距离 `0.603 m`，安全降速 3 次，
  全局规划消息 21 条，测试后成功返航。
- 多点巡航曾出现到点左右摆头；旧验收只判断顺序和最终误差，现已增加
  到点前低速阶段的角速度反向次数与无效往返转角硬门禁。
- 最终实现不再逐点取消并重发 `NavigateToPose`：任务层直接调用
  `ComputePathThroughPoses` 生成连续路径，并用巡航专用
  `Regulated Pure Pursuit` 跟踪；单点导航继续使用 DWB。
- 闭环路线在回家方向设置距 `home` 约 `0.45 m` 的同向交接点，最后一段仍由
  Pure Pursuit 直行；进入 `home` 的 `0.18 m` 范围后取消尾段并平滑停车，
  不执行精确终点朝向旋转。
- 中间航点采用 `0.25 m` 通过半径；验收从进入航点 `0.30 m` 区域开始采样，
  在任务管理器确认该点完成时立即封存，避免把下一段路线的正常转向误算为
  上一个点摆头。反向次数和 `0.12 rad` 无效往返转角门槛没有放宽。
- 修正前基线：`northeast` 反向 4 次，`north/home` 各 1 次，`home`
  无效往返转角 `0.3921 rad`，判定不合格。
- 最终连续复验第 1 轮：四点最近距离 `0.089 / 0.090 / 0.060 / 0.031 m`，
  反向次数全部为 0，终点无效往返转角 `0.0096 rad`，总耗时
  `29.755 s`，最终原点误差 `0.173 m`，门禁合格。
- 最终连续复验第 2 轮（不重启环境）：四点最近距离
  `0.154 / 0.117 / 0.060 / 0.127 m`，反向次数和无效往返转角全部为 0，
  总耗时 `27.002 s`，最终原点误差 `0.180 m`，门禁合格。
- 随机鲁棒巡航现可从任务 JSON 接收任意航点，并自动生成旋转、轻度倾斜的
  四边形闭环路线；每批固定覆盖顺时针和逆时针，打印 seed 以便复现。
- 巡航规划链升级为 `SmacPlanner2D → SimpleSmoother → Regulated Pure
  Pursuit → Velocity Smoother → 单向转向保护 → Collision Monitor`。只有
  航点导向明确、规划路径未明显绕障的多点主路线才启用保护；首段保留自由
  对准，进入第一个航点后启用，最后单点回家段自动解除。
- 2026-08-29 固定路线回归：`4/4` 到点，反向次数和无效往返转角全部为 0，
  最终原点误差 `0.176 m`，门禁合格。
- 2026-08-29 两组独立随机种子 `1025773903`、`3316381630`：共 `10/10`
  条路线、`40/40` 个到点通过；所有到点反向次数为 0，最大无效往返转角
  `0.0564 rad`，最大原点误差 `0.179 m`，均满足硬门槛。
- 2026-08-29 动态避障升级为随机鲁棒验收：每轮从可通行区最远候选中随机
  选择目标（距离至少 `2.00 m`），底盘先实际行驶至少 `0.25 m`，再沿实时
  `/plan` 随机生成 `1–4` 个 Gazebo 障碍物。一个主障碍压住原路径以强制
  绕行，其余障碍随机分布在同侧外圈，避免生成无解的横向障碍墙。
- 动态模式使用独立的 `Regulated Pure Pursuit` 弧线控制器，关闭原地朝向
  旋转；行为树不包含 `Spin`，动态重规划频率为 `3 Hz`。验收新增硬门禁：
  单次原地转向 `≤ 1.50 s`、累计原地转角 `≤ 0.70 rad`，并沿实际外出轨迹
  倒车返航，避免每轮测试之间原地掉头。
- 最终代码下，两组独立随机种子 `2198384536`、`341120867` 共 `6/6` 通过。
  六轮终点距离范围 `2.623–3.104 m`，最大到点误差 `0.073 m`，最小障碍
  中心距离 `0.551 m`，插障后每轮重规划 `49–56` 次，最大返航误差
  `0.246 m`。五轮原地转向为 `0`，一轮仅 `0.090 s / 0.016 rad`。
  验收会打印主 seed 和每轮 seed，可精确重放失败场景。
- 2026-08-30 可视化复验曾由 seed `3352151224` 暴露 `GridBased` 贴障折线路径
  导致 Pure Pursuit 连续预测碰撞、控制耐心耗尽并中止。动态行为树已改用
  平滑的 `SmacPlanner2D (PatrolGrid)`，仍保持 `3 Hz` 重规划和禁止原地朝向
  旋转；随后随机种子 `3047279837` 连续 `3/3` 通过。三轮到点误差
  `0.038–0.073 m`，最小障碍中心距离 `0.620 m`，插障后重规划 `54–61`
  次，三轮原地转向时间和累计转角均为 `0`，返航误差 `0.242–0.246 m`。
- 2026-08-30 真实视觉触发复验发现：若在前往二维码站之前就开启一次性视觉
  授权，沿途的蓝色事件会先消费授权并错误生成 `blue_station` 任务。验收流程
  现改为接近阶段保持关闭，到达 `qr_station` 并停车后才授权，只统计授权后
  的新事件。修正后摄像头现场读出 `NAV:HOME`，任务系统自动生成 `home`，
  队列清空并返航；总用时 `99.404 s`，最终原点误差 `0.074 m`，输出
  `VISION_TASK_LOOP_VALIDATION_OK`。
- Gazebo 图形启动入口现跳过在线模型库并显式使用项目本地模型目录；已实际
  解除 `Preparing your world`/黑屏，图形窗口恢复后约 `62 FPS`。
- 当前显示布局固定为右侧 24 寸副屏：Gazebo、RViz 和验收面板的启动坐标
  均避开 `2560×1440` 主屏；若 Windows 以后调整显示器排列，需要同步更新
  三处窗口坐标。
- 2026-08-30 验收面板新增“跨机联调配置”：本机保存 WSL 发行版、Linux
  工作区、GitHub 地址、目标分支和源码目录；支持环境检测、Clone/仅快进更新、
  ROS2 Humble 基础环境安装、`rosdep` 依赖安装、`colcon` 构建，以及经文件
  清单和二次确认后的 GitHub commit/push。路径不再依赖固定 WSL 用户名，
  本机配置、运行日志、常见密钥和 Token 不进入 Git。
- 当前仓库尚未配置 `origin`，GitHub 地址保持待用户填写；在仓库地址明确前，
  未执行任何远端创建、Clone 或 push，不能把“发布入口已完成”表述成“已上传”。

仍然只代表 Gazebo 仿真通过；实体底盘需要重新标定有效轮距、轮胎摩擦、
编码器比例和电机闭环参数，不能直接照搬仿真数值。

## PPO 工程扩展状态（2026-08-15）

已完成：

- 独立 Python 虚拟环境：PyTorch `2.8.0+cpu`、Gymnasium `1.3.0`、
  Stable-Baselines3 `2.9.0`。
- 新增 `zhirong_ppo` ROS2 包和 Gymnasium 环境。
- 66 维观测：前方 180° 雷达 60 维、目标距离/方位、当前速度和上一动作。
- PPO 输出为归一化连续动作，映射到 `0～0.30 m/s` 和
  `-0.60～0.60 rad/s`。
- 安全链固定为 `/cmd_vel → Collision Monitor → /cmd_vel_safe → 底盘`。
- 支持空场和单障碍课程环境、Gazebo 自动复位、随机目标、奖励分解、
  超时/成功/碰撞判定。
- 可复现训练、独立评估、模型保存、训练曲线、逐回合指标和配置归档。
- 每 `256` 环境步自动保存恢复检查点，支持断点模型、学习率和熵系数调整。
- 新版 `v2_bearing_half_pi` 观测从零训练，避免与旧模型的特征尺度混用。
- Reward V4 增加对称转向整形，并在目标偏离车头时抑制继续高速前冲。
- 环境检查、单障碍冒烟、模型落盘后重新加载均通过。
- 首轮 `1024` 步短训练完成；3 轮评估成功率 `33.3%`、碰撞率 `0%`。
- 修复完整系统 GUI 重启时的生命周期竞态，`velocity_smoother` 现可自动
  进入 `active`；重启后 `FULL_SYSTEM_HEALTH_OK`。

该结果只证明工程闭环可用，不证明 PPO 优于 DWB。在完成同场景、冻结参数的
正式对比前，PPO 不替换稳定主控制器。

后续课程实验已完成 Reward V2/V3/V4、直行、扇形、随机空场、早期单障碍失败
实验、低学习率微调和两种观测缩放 A/B：

- 新观测直行：`10/10`，碰撞 `0/10`
- Reward V4 固定扇形正式评估：`24/30`（`80%`），碰撞 `0/30`
- Reward V4 随机空场最佳检查点：`21/30`（`70%`），碰撞 `0/30`
- 最佳随机空场模型：
  `artifacts/ppo/20260815_v5_empty_transfer_seed17/checkpoints/ppo_zhirong_10240_steps.zip`

随机空场未达到 `80%` 晋级门槛，因此本轮不进入单障碍正式训练，也不替换
DWB。详细指标、模型 SHA256 和失败分析见
`docs/PPO_TRAINING_REPORT_2026-08-15.md`。
