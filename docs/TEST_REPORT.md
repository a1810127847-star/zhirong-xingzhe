# 最终测试报告

测试时间：2026-08-15 09:37:34～09:41:53（Asia/Shanghai）

完整日志：

```text
artifacts/validation/full_regression_latest.log
```

## 结论

计划书定义的一级、二级功能和三类演示任务全部通过自动回归。最终回归从
全量构建开始，在独立无 GUI Gazebo 会话中依次完成单元测试、系统健康检查、
多点巡航、动态避障、安全急停、视觉返航、失败恢复和转向定位稳定性门禁，
并在结束前再次检查关键生命周期节点。

最终标志：

```text
FULL_REGRESSION_OK
```

## 构建与单元测试

| 项目 | 结果 |
|---|---:|
| ROS 2 包构建 | 5/5 成功 |
| 任务解析/映射测试 | 5/5 通过 |
| 颜色/二维码算法测试 | 3/3 通过 |
| 目标安全算法测试 | 6/6 通过 |
| `colcon test-result` 汇总 | 15 个结果，0 错误，0 失败 |

## 系统健康

| 组件 | 结果 |
|---|---|
| Collision Monitor | `active [3]` |
| AMCL | `active [3]` |
| BT Navigator | `active [3]` |
| Velocity Smoother | `active [3]` |
| Gazebo 动态障碍 | 存在 |
| Gazebo `NAV:HOME` 二维码 | 存在 |
| 任务管理 | `IDLE`、视觉默认关闭、一次性触发开启 |
| 实时视觉 | 初始视野稳定识别蓝色标志 |

## 导航与任务指标

| 验收项 | 指标 | 结果 |
|---|---:|---|
| 点到点导航 | 目标 `(0.8, 0.0)` | `SUCCEEDED` |
| 点到点导航 | 近似到达误差 | `0.105 m` |
| 四站点巡航 | 路线 | `east → northeast → north → home` |
| 四站点巡航 | 连续两轮用时 | `29.755 s / 27.002 s` |
| 四站点巡航 | 连续两轮返航误差 | `0.173 m / 0.180 m` |
| 四站点巡航 | 航点最大最近距离 | `0.154 m` |
| 四站点巡航 | 四点转向反复 | 两轮均为 `0` |
| 四站点巡航 | 最大无效往返转角 | `0.0096 rad` |
| 视觉返航 | 二维码 | `NAV:HOME` |
| 视觉返航 | 用时 | `37.936 s` |
| 视觉返航 | 返航误差 | `0.036 m` |

## 转向定位稳定性

针对 RViz 转向抖动进行了 Gazebo 真值、`/odom` 和 AMCL 三路对照测量。
保留真实的四轮滑移转向物理特性，不通过切换 RViz 固定坐标系隐藏误差。

| 指标 | 最终实测 | 回归上限 |
|---|---:|---:|
| AMCL 运动噪声 `alpha1～alpha5` | `0.02` | — |
| Nav2 最大角速度 | `0.6 rad/s` | — |
| Nav2 最大角加速度 | `1.2 rad/s²` | — |
| 原地转向角度 | `1.5438 rad` | 最低 `0.80 rad` |
| Gazebo 物理位置漂移 | `0.1007 m` | `0.15 m` |
| 里程计最大位置误差 | `0.0104 m` | `0.04 m` |
| AMCL 最大单步位置修正 | `0.0091 m` | `0.03 m` |
| AMCL 航向均方误差 | `0.0417 rad` | `0.08 rad` |

结论：转向定位误差已纳入自动回归并通过。四轮滑移转向产生的约 `10 cm`
物理位移仍存在；这是当前仿真底盘物理特性，不能通过 RViz 显示设置消除。

## 动态避障

动态障碍放置在原导航路径上，8 秒后移到安全区域。

| 指标 | 结果 |
|---|---:|
| 任务结果 | 成功到达并返航 |
| 到达误差 | `0.076 m` |
| 导航用时 | `20.103 s` |
| 最大横向绕行 | `0.240 m` |
| 前方最近激光距离 | `0.554 m` |
| 机器人—障碍最近中心距 | `0.731 m` |
| 安全降速采样 | `2` |
| 全局规划消息 | `19` |

## Collision Monitor

| 场景 | 指标 | 结果 |
|---|---|---:|
| 前进轨迹被障碍占据 | 最大安全输出速度 | `0.017 m/s` |
| 前进轨迹被障碍占据 | 机器人位移 | `0.018 m` |
| 前方有障碍、旋转扫掠安全 | 最大安全角速度 | `0.500 rad/s` |
| 前方有障碍、旋转扫掠安全 | 转角 | `0.562 rad` |
| 后方净空 | 最大安全后退速度 | `0.120 m/s` |
| 后方净空 | 后退位移 | `0.139 m` |
| 障碍移开 | 最大安全输出速度 | `0.200 m/s` |
| 障碍移开 | 机器人位移 | `0.235 m` |

方向感知阻挡、转向、后退和自动恢复均通过。

## 目标安全入口

| 场景 | 结果 |
|---|---|
| 橙色圆柱旁目标 `(2.22, -1.16)` | 移动前拒绝；最近致命障碍约 `0.09 m` |
| 安全 `home (0.0, 0.0)` | 接受、转发并成功到达 |
| `/navigate_to_pose` 服务端数量 | `1`，仅 `/goal_guard` |
| `/nav2_raw/navigate_to_pose` 服务端数量 | `1`，仅 `/nav2_raw/bt_navigator` |

## 失败恢复

测试目标 `(4.5, 4.5)` 位于地图外：

1. 第一次导航失败。
2. 状态机自动重试一次。
3. 第二次失败后进入 `FAILED` 并记录错误。
4. 不重启系统，发送有效 `east` 任务。
5. 有效任务成功，随后返航。

| 指标 | 结果 |
|---|---:|
| 不可达目标尝试次数 | `2` |
| 失败确认时间 | `1.168 s` |
| 恢复后有效任务时间 | `3.755 s` |
| 最终返航误差 | `0.079 m` |

## 传感器基线

| 传感器 | 已验证结果 |
|---|---|
| LiDAR | 720 点、360°、约 9.98 Hz、`lidar_link` |
| RGB | 320×240 `rgb8`、约 15 Hz |
| Depth | 320×240 `32FC1` |
| PointCloud2 | 320×240，含 `x/y/z/rgb` |
| SLAM | 119×119、0.05 m/cell、`map → odom` 正常 |

## 测试边界

- 所有运动和安全数据来自 Gazebo 仿真。
- 未连接真实底盘、雷达、相机或机械臂。
- 手柄功能按用户要求保持暂停。
- PPO、语音、VLA/VLM、机械臂和实车属于三级扩展，不阻塞本次一级、二级验收。

## 最终视觉巡检复验

在完整回归之后，又针对任务累计计数、单任务超时和视觉站点安全距离进行了
专项复验。蓝色站点调整到 Collision Monitor 预测碰撞范围之外，Nav2 启动延后到
Gazebo 传感器稳定后，并增加最长 `90 s` 的单任务超时与一次自动重试。

| 指标 | 结果 |
|---|---:|
| 路线 | `blue_station → qr_station → home` |
| 三个任务结果 | 全部 `SUCCEEDED` |
| 总用时 | `50.837 s` |
| 最终地图坐标 | `(-0.028, -0.042)` |
| 返航误差 | `0.050 m` |
| 演示视频 | 320×240、15 FPS、1051 帧、70.067 s |

最终视频：`artifacts/demo/zhirong_complete_demo_final.mp4`。
